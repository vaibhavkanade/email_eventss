require 'spec_helper'

describe EmailEvents do
  it 'has a version number' do
    expect(EmailEvents::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
